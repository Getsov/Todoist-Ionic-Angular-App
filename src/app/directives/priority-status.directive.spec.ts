import { PriorityStatusDirective } from './priority-status.directive';

describe('PriorityStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityStatusDirective();
    expect(directive).toBeTruthy();
  });
});
